<%= form_for [:adjust, light] do |f| %>

  <%= f.label :hue %>
  <%= f.number_field :hue %><br>
  
  <%= f.label :brightness %>
  <%= f.number_field :brightness %><br>

  <%= f.submit 'pas licht aan' %>

<% end %>
